#pragma once

#include <cstring>
#include <cstdlib>
#include <cstdio>
#include <string>
#include <vector>
#include <iostream>

namespace utils {
	bool isNumber(char);
	bool isGroupStart(char);
	bool isGroupEnd(char);
};